## Implementar MCP (Model Context Protocol)
- Configurar a estrutura de arquivos e diretórios.
- Implementar memória local com `.cursor-memory/memory.json`.
- Criar métodos `add`, `list` e `clear` para eventos de memória no padrão JSON especificado.
